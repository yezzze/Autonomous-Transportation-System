apiVersion: v1
kind: Service
metadata:
  name: agent-grpc
spec:
  selector:
    app: agent-grpc
  type: NodePort
  ports:
    - name: grpc
      protocol: TCP
      port: 50051
      targetPort: 50051
      nodePort: 30051
