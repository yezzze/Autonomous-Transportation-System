## 应用层

---

### §1 安装/卸载应用 ✅ 主流程已实现，AW→ARDC 路径为骨架

| 步骤                        | 接口文档 | 实现状态                                                     |
| --------------------------- | -------- | ------------------------------------------------------------ |
| APPM→ALRE 安装/卸载执行逻辑 | ✅        | `install_app_logic()` / `uninstall_app_logic()` + 磁盘持久化 |
| APPM→AW 安装/卸载智能体镜像 | ⚠️ 骨架   | `agent_warehouse` 存在但不实际拉取镜像                       |
| AW→ARDC 注册/注销           | ⚠️ 骨架   | `agent_registry.json` mock，无真实服务注册                   |

---

### §2 启动应用 ✅ 主流程已实现，ORCH→RUN 为骨架

| 步骤                         | 接口文档 | 实现状态                                                     |
| ---------------------------- | -------- | ------------------------------------------------------------ |
| APPM→ALRE→ORCH 开始编排      | ✅        | `start_app()` → asyncio.Task → `run_distributed_workflow()`  |
| ORCH→RUN 部署智能体 + 拉镜像 | ⚠️ 骨架   | `AgentScheduler` 已支持优先读取 `k8s/*.yaml` 创建 `Deployment/Service`，失败时回退到字典构造 |
| ORCH 启动工作流              | ✅        | LangGraph 工作流正常运行（所有节点均为 async def，支持多工作流并发） |
| 返回应用接口                 | ✅        | 返回 `workflow_handle`                                       |

补充（2026-05-25）：`run_distributed_workflow()` 已支持外部 `workflow_id` 参数；`AppLogicEngine._run_workflow()` 调用时会把 `workflow_handle` 透传为 `workflow_id`，用于对齐应用层句柄与可视化工作流 ID。

---

### §3 停止应用 ✅ 主流程已实现，RUN 引用计数为骨架

| 步骤                    | 接口文档 | 实现状态                                                     |
| ----------------------- | -------- | ------------------------------------------------------------ |
| APPM→ALRE→ORCH 停止编排 | ✅        | `stop_app()` → `task.cancel()`                               |
| 工作流引用自减          | ⚠️ 骨架   | `_unsubscribe_instances()` 调用 ALCM.unsubscribe()，内存计数 |
| RUN 引用为0时关闭智能体 | ⚠️ 骨架   | ALCM 逻辑在内存中，无实际容器/进程关闭                       |

---

### §4 交互呈现 ✅ 已实现（含 Skills.md 在线编辑）

| 步骤                              | 接口文档 | 实现状态                                                                   |
| --------------------------------- | -------- | -------------------------------------------------------------------------- |
| DISP→APPM 请求运行应用列表        | ✅        | `/api/apps/` + `/api/apps/running`                                         |
| DISP 调用应用接口交互             | ✅        | `/api/apps/{id}/interface` + Web UI；应用详情页已合并可视化三 tab（编排过程/拓扑结果/执行监控），通过 `/api/viz/*` + `/ws/viz/*` 实时渲染 |
| DISP→APPM 查询单个应用详情        | ✅        | `GET /api/apps/{app_id}` 已实现                                            |
| DISP→APPM 更新应用内容            | ✅        | `PATCH /api/apps/{app_id}` + `AppManager.update()` 已实现                  |
| UI 内联编辑 Skills.md             | ✅        | 应用列表每行 ✏️ 按钮，点击展开 textarea 内联编辑器，保存调用 PATCH 接口    |
| 编辑中防止刷新丢失内容            | ✅        | `_editSkills` 字典缓存用户输入，5 秒自动刷新不覆盖未保存内容               |
| 应用列表刷新结果去重              | ✅        | `loadApps()` 对 `apps` 结果做签名比对，相同结果直接跳过重绘                |
| 应用详情页智能体前端视图          | ✅        | `/ui/apps/{app_id}` 的“智能体视图”标签页按运行中 Agent 纵向堆叠 iframe，并支持打开独立页面 |
| 应用详情页 tabs 重构（5 tab）     | ✅        | tab 顺序已固定为“逻辑文件 → 编排过程 → 拓扑结果 → 执行监控 → 智能体视图”；原“运行状态”tab 已下线 |
| Demo 智能体视图本地可访问        | ✅        | 对已知 `192.168.49.2:30092` 视图自动改写为 `127.0.0.1:30092`，配合本地 `kubectl port-forward` 使用 |

---

**总结**：接口文档定义的**主流程全部实现**，并新增了 Skills.md 在线编辑能力。AW（智能体镜像仓库）→ARDC（注册中心）以及 RUN（运行层容器管理）这条链路是骨架/mock，属于生产环境才需要替换的基础设施部分。



## 编排层

---

### §1 智能体发现（ARDC 广播） ✅ 已实现（HTTP Gossip 协议）
终端↔边缘双向广播 Agent 列表：`src/service/ardc_gossip.py` 实现 HTTP Gossip 协议，`agent_server.py` 新增 `POST /registry/sync` 端点，`agent_registry.py` 集成分布式视图。节点启动时通过 `PEER_AOE_URLS` 环境变量加入 Gossip 网络，每隔 30s 同步各节点 Agent 列表。Agent 查询采用精确匹配（HNSW 向量检索已于 2026-03-29 移除）。

**Agent 本地/远端判定统一**：`agent_registry.json` 新增 `is_local` 字段后，`src/api/visualization.py`、`src/graph/distributed_nodes.py` 与 `src/service/agent_startup.py` 已改为以注册表数据为唯一来源判断本机/远端，避免再维护分散的 localhost / 127.0.0.1 硬编码集合。

---

### §2.1 单主体工作流编排 ✅ 主流程已实现，RRDC 为骨架

| 步骤                                       | 实现状态                                               |
| ------------------------------------------ | ------------------------------------------------------ |
| AOE 接收指导文件、请求 ARDC 智能体列表     | ✅ `distributed_planner_node` 调用 `agent_registry`     |
| AOE 筛选并编排智能体                       | ✅ LLM Planner 或 Pipeline 快速路径                     |
| ASD 部署智能体（含 RRDC 资源申请）         | ✅/⚠️ `agent_scheduler.py` 根据后端选择 `subprocess` 或 Kubernetes；Kubernetes 后端优先读取仓库根目录 YAML。2026-05-07 已将两个示例 Deployment 补齐 CPU/内存 requests+limits，并显式声明 `nvidia.com/gpu` requests+limits 以适配共享 GPU 场景；同日完成 NVIDIA device plugin `time-slicing` 实测，单节点 `nvidia.com/gpu` Capacity 从 1 生效为 4，两个 GPU Pod 可同时 Running |
| AWM 生成并启动工作流                       | ✅ LangGraph 工作流（`build_distributed_graph()`）      |
| AWM 订阅 Agent，引用计数 +1                | ✅ `lifecycle_manager.subscribe()` 内存引用计数         |
| AWM 循环调用 Agent                         | ✅ `distributed_executor_node` + UnifiedExecutor        |

---

### §2.2 跨主体工作流编排 ✅ 主流程已实现

| 步骤                                               | 实现状态                                                     |
| -------------------------------------------------- | ------------------------------------------------------------ |
| D_AOE 拆解任务图，分发子任务给 E_AOE               | ✅ `dispatch_subtask_to_remote_aoe()` → `POST /orchestration/dispatch` |
| E_AOE 接收子任务，验证智能体，启动独立工作流       | ✅ `agent_server.py` `/orchestration/dispatch` 端点，`asyncio.create_task` 启动工作流 |
| 工作流引用次数自增 / E_AOE 返回 workflow_handle    | ✅ `_SessionRegistry` 记录 session→handle→task               |
| D_AOE 生成含远程接口的本地工作流                   | ✅ `cross_host_sessions` state 字段，executor 路由到远端     |
| T_AOE 收到结果后通知 E_AOE 清理（`DELETE /session`）| ✅ `_cleanup_remote_session` 异步调用                        |

---

### §2.3 跨主体重编排（Agent 失效） ✅ 已实现

| 步骤                                                    | 实现状态                                                     |
| ------------------------------------------------------- | ------------------------------------------------------------ |
| 检测远端 E_AOE 失效（timeout / error）                  | ✅ `dispatch_subtask_to_remote_aoe()` 返回 `status=error/timeout` 时标记失败 |
| 记录已失败的远端节点 URL                                | ✅ `failed_remote_aoe_urls: Dict[str, List[str]]` 写入 DistributedState |
| 查询 ARDC+Gossip 寻找替代节点                           | ✅ `find_alternative_remote_aoe(task, failed_urls)` — 同 capability，跳过本地和已失败 IP |
| Rule 0（最高优先级）：切换到替代节点重新分发            | ✅ `apply_failure_rules` Rule 0 更新 `cross_host_sessions`，重置 `current_task_index` |
| Rule 1 不在已失败跨主体任务上重试同节点                 | ✅ Rule 1 跳过 `failed_cross_host_tasks` 中的任务 |
| 无替代节点时降级 → LLM 重规划                           | ✅ Rule 0 返回 `handled=False`，monitor 触发 `trigger_llm_replanning` |

---

### §2.3 单主体工作流停止编排 ✅ 主流程已实现，ASD 注销为骨架

| 步骤                             | 实现状态                            |
| -------------------------------- | ----------------------------------- |
| AOE 停止工作流                   | ✅ `task.cancel()`                   |
| AWM 工作流引用次数自减           | ✅ 内存引用计数                      |
| AWM 退订 Agent，引用归零自动关闭 | ✅ `lifecycle_manager.unsubscribe()` |
| ASD 关闭实例 → ARDC 注销         | ⚠️ mock，不执行真实容器关闭          |
| RES 释放资源                     | ⚠️ resource_registry.py 内存 mock    |

---

### §2.4 跨主体工作流停止编排 ✅ 已实现

| 步骤                                               | 实现状态                                                     |
| -------------------------------------------------- | ------------------------------------------------------------ |
| D_AOE stop_app() 通知远端 E_AOE 终止子任务         | ✅ `stop_app()` fire-and-forget `_cancel_remote_session()` → `DELETE /orchestration/session/{id}` |
| E_AOE 收到 DELETE，取消正在运行的工作流 Task       | ✅ `_SessionRegistry.cancel_session()` → `task.cancel()`，dispatch handler 捕获 CancelledError 返回 `{"status":"cancelled"}` |
| E_AOE ALCM 退订，引用计数自减                      | ✅ `close_session()` 端点完成 ALCM unsubscribe                |

---

### §2.2+ 跨主体编排主动触发 ✅ 已实现（2026-05-07）

| 步骤 | 实现状态 |
| --- | --- |
| Planner 出口调用 `identify_cross_host_tasks()` | ✅ Pipeline 路径和 LLM 路径两个出口均注入调用 |
| 识别结果写入 `cross_host_sessions` | ✅ 通过 Command(update={...}) 传递给 state |
| `distributed_workflow.py` 初始化跨节点 state 字段 | ✅ `cross_host_sessions`/`failed_cross_host_tasks`/`failed_remote_aoe_urls` |

---

### 子工作流即服务（Sub-Workflow as a Service） ✅ 已实现（2026-05-07）

| 步骤 | 实现状态 |
| --- | --- |
| 子工作流定义配置 `config/sub_workflows.json` | ✅ 复用 PipelineTopology 格式，支持串行+并行组 |
| `SubWorkflowInfo` TypedDict | ✅ `distributed_types.py` 新增，含 id/capability/description/owner_ip/owner_port/pipeline/status |
| `TaskAssignment.sub_workflow_id` | ✅ 非空时表示任务路由到子工作流 |
| `agent_registry.py` 加载子工作流 | ✅ `_load_sub_workflows()` 从 config 加载，补充 owner_ip/port |
| ARDC Gossip 同步子工作流 | ✅ `push_to_peer()` 携带 `sub_workflows`，`sync_from_peer()` 接收存储 |
| Planner 展示子工作流给 LLM | ✅ `_build_sub_workflow_prompt_section()` 生成 prompt 段落 |
| Executor 三路径路由 | ✅ 远端跨主体 → 本地子工作流 → 本地单 Agent |
| E_AOE 接收子工作流分发 | ✅ `agent_server.py` dispatch 检查 `sub_workflow_id`，查找本地 pipeline 执行 |
| `identify_cross_host_tasks()` 支持子工作流 | ✅ 使用 target_ip（owner）判断跨节点 |

---

### 周期性工作流调度 ✅ 已实现（2026-05-07）

| 步骤 | 实现状态 |
| --- | --- |
| WorkflowScheduler 单例调度器 | ✅ `src/service/workflow_scheduler.py`，asyncio while-loop 调度循环 |
| ScheduleExecutionRecord 数据模型 | ✅ `src/app/models.py` 新增，含 run_id/status/timestamps/result_summary |
| AppStatus "scheduled" 状态 | ✅ `models.py` AppStatus 扩展 |
| GuidanceFile.constraints 调度扩展 | ✅ `schedule_interval_seconds` / `schedule_max_parallel` / `schedule_auto_restart` |
| AppManager 调度生命周期 | ✅ `start_schedule()` / `stop_schedule()` / `restore_schedules()` |
| AppLogicEngine 暴露单次执行 | ✅ `run_single_workflow()` 供调度器调用 |
| 4 个调度 API 端点 | ✅ `schedule/start` / `schedule/stop` / `schedule/status` / `schedule/history` |
| FastAPI startup 自动恢复 | ✅ `_restore_scheduled_apps()` 恢复 `schedule_auto_restart=true` 的应用 |
| 执行历史持久化 | ✅ `data/schedule_history.json`，按 app_id 分组 |
| Web UI 调度控制 | ✅ 安装表单调度字段 + ⏱ 定时启动/⏹ 停止调度按钮 + 📋 历史面板 |
| 调度会话聚合可视化 | ✅ 调度启动时创建 `schedule_workflow_handle` 主记录，周期子工作流不再逐条注册可视化，`/viz` 主列表按会话聚合展示 |
| 周期执行-会话追踪关联 | ✅ `ScheduleExecutionRecord` 新增 `schedule_workflow_handle` 字段，单次 run 可追溯到所属调度会话 |
| 调度前端刷新抑制 | ✅ `static/visualization.html` 对 Pane1 启用“按工作流签名缓存 + 调度空编排快照忽略”，周期触发不再回退 Skills/Pipeline 显示 |
| APPM 调度句柄回填 | ✅ `AppManager.start_schedule()` 成功后将 `schedule_workflow_handle` 回填到 `app.workflow_handle`，`stop_schedule()` 停止后清空，调度与普通启动句柄语义对齐 |

---

**总结**：

- ✅ **已实现**：单主体完整工作流（编排→部署→执行→停止）、ARDC Gossip 跨节点发现（含子工作流同步）、ASD subprocess 进程管理、§2.2 跨主体编排分发（含主动触发）、§2.3 跨主体重编排（Agent 失效 failover）、§2.4 跨主体停止传播、引用计数生命周期、子工作流即服务、周期性工作流调度
- ⚠️ **骨架/Mock**：RRDC 资源分配（内存实现）、AW 镜像拉取（本机 subprocess 替代）

  



## 运行层

---

### §1 智能体部署与调用

| 组件                            | 接口文档角色                  | 实现状态                                                     |
| ------------------------------- | ----------------------------- | ------------------------------------------------------------ |
| **ALCM**                        | 部署实例、引用计数、订阅/退订 | ✅ lifecycle_manager.py 完整实现（内存）                      |
| **SBOX**（执行沙箱）            | 创建实例、运行 Agent 逻辑     | ⚠️ 无独立沙箱，Agent 直接通过 `UnifiedExecutor` 调用，没有隔离容器 |
| **COMM**（跨 Agent 通信中间件） | Agent 间通信、访问大模型      | ⚠️ 无独立 COMM 层，大模型访问靠 `LLM Simulator` / A2A 协议直接调用，无中间件抽象 |
| **INTF**（传感器/执行器接口）   | 传感器读取、执行器命令        | ✅ resource_interface.py 有结构，但全部是 mock，无真实硬件对接 |
| **RES**（资源抽象层）           | 资源分配/释放                 | ✅ resource_registry.py + ALCM `_allocate_resources()` 有内存实现 |
| **AW→ALCM 拉取镜像**            | 实际拉取镜像文件              | ⚠️ mock，不执行真实拉取                                       |

---

### §2 业务-资源智能体协同

| 组件                       | 接口文档角色                               | 实现状态                                                     |
| -------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| **QOS**                    | 上报 QoS 日志、统计查询、供资源 Agent 读取 | ✅ qos_monitor.py 完整实现（内存统计、告警阈值、`record_call()`） |
| **QoS → ASD 反馈闭环**     | QoS 告警回调 → AgentScheduler.redeploy_agent() | ✅ agent_server.py 启动注册回调，冷却 60s 防 storm，redeploy 后 reset 指标；5 个集成测试全覆盖 |
| **SBOX1↔COMM↔SBOX2 协同**  | 业务 Agent 与资源 Agent 双向通信           | ❌ 未实现，无资源 Agent 概念，Agent 间无 COMM 中间件协同      |
| **SBOX2 调度通信计算资源** | 根据 QoS 动态调度资源                      | ⚠️ 部分实现：QoS 告警已触发 ASD redeploy，但无细粒度资源动态调度 |

---

**总结**：

- ✅ **已实现**：ALCM 生命周期（部署/订阅/引用计数/关闭）、QoS 监控采集、**QoS → ASD 反馈闭环（告警→自动重部署）**、INTF/RRDC 结构骨架
- ⚠️ **Mock/骨架**：SBOX 执行隔离（无容器）、INTF 硬件对接（返回 mock 数据）、镜像拉取
- ❌ **未实现**：COMM 跨 Agent 通信中间件、业务-资源 Agent 协同、QoS 驱动的动态资源调度

运行层整体是**管理框架完备、执行层 mock**的状态，核心缺失是真实的 SBOX 容器隔离和 COMM 中间件，这两块是生产环境需要对接的基础设施。

---

