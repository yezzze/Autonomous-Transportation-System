# 边缘 NATS 部署默认值（setup_edge_nats_helm.sh 自动加载）
# 覆盖方式：export NATS_CLOUD_HOST=... 或编辑本文件

# 云端 NATS Hub 对边缘 Pod 可达的 IP（本机集群 A / 云端 minikube 宿主机）
NATS_CLOUD_HOST=10.112.136.44

# 当前边缘集群业务 ID
EDGE_CLUSTER_ID=edge-a

# 与云端 nats-cloud-values.yaml leafnode 密码一致
NATS_CLOUD_PASSWORD=change-me-leaf-password
